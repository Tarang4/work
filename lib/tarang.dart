import 'package:flutter/material.dart';

class Trang extends StatelessWidget {
  const Trang({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lightBlue,
    );
  }
}
